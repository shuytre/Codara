{
  "name": "@codara/renderer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --root ../tests --project renderer --passWithNoTests"
  },
  "dependencies": {
    "@codara/contract": "workspace:*",
    "dompurify": "^3.4.16",
    "marked": "^18.0.14",
    "solid-js": "^1.8.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vite-plugin-solid": "^2.10.0",
    "vitest": "^1.6.0"
  }
}
