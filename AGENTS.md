# AGENTS.md — Codara 开发约束（每次会话自动注入）

> 本文件只含元指令、硬约束摘要与文档索引。完整规格见 `docs/spec/codara-spec.md`，禁止每会话全读规格。

## 第 0 章 元指令（最高优先级）

0.1 **定位红线**：Codara 是对话式自然语言驱动的编程 Agent，不是 IDE。任何把它往"代码编辑器/IDE"方向做的设计都是方向性错误，必须拒绝。

0.2 **兼容红线**：任何依赖、API、语法、构建配置引入前，必须先过硬约束红线；不能确认则写《兼容性风险登记表》并给替代方案，禁止侥幸引入。

0.3 **专家团红线**：一个底座模型 + 多个角色化独立会话。角色会话物理隔离、历史互不可见，只通过调度器与结构化交接物通信。

0.4 **模式红线**：极简长编码模式是官方默认模式。该模式下模型只可见 `read / write / terminal / git / search` 五类工具，默认 shell 为 cmd.exe。新增工具或模式必须经人类批准。

0.5 **先计划后执行**：每个里程碑先产出 WBS、影响文件清单、测试计划，人类确认后再写代码。

0.6 **小步闭环**：单次改动可审查；功能点自测通过再继续；禁止堆积未验证代码。

0.7 **事实纪律**：版本号、兼容性结论、跑分数据必须有可追溯来源；不确定写 `【待验证】`，禁止编造。

0.8 **失败处理**：同一报错连续出现 2 次，停止重试，做根因分析（RCA）。

0.9 **交付要素**：每轮交付包含改动清单、决策理由、自测结果、遗留问题、下一步。

0.10 **破坏性操作**（删文件、改 Git 历史、对外网络、安装系统级软件）必须暂停请求人类确认。

0.11 **安全诚实**：软沙箱不是内核级安全边界；文档与 UI 中不得做出"绝对隔离/绝对防住"的表述。

0.12 **首要用户红线**：一切设计以个人开发者"自有电脑、愿装愿用、开箱即用"为基准。

## 硬约束摘要（完整版见规格第 5 章）

| 项 | 锁定 |
|---|---|
| OS | 仅 x64；Win7 必须 SP1；32 位与 Win7 RTM 明确不支持 |
| 桌面壳 | Electron 22.3.x 终版（Chromium 108 / Node 16.17）；23+ 禁用 |
| 主进程 | TypeScript，target ≤ ES2020，禁 Node 17+ API（无全局 fetch，用 https 模块） |
| 渲染层 | SolidJS（决策覆盖 #3），构建目标 chrome/108 |
| Sidecar | 独立进程 exe，stdio 行分隔 JSON-RPC，Rust 1.77.2（禁 1.78+ 特性） |
| 存储 | rusqlite bundled 静态链接；Node 侧零原生 DB 依赖 |
| Git | MinGit x64 2.46.2 随包 |
| 终端 | node-pty（Electron 22 匹配 ABI），默认 shell cmd.exe |
| Key | DPAPI 按用户加密（经 sidecar），不写明文 |
| 网络 | 国产直连主打；遥测默认关；应用运行时禁止联网下载二进制 |
| 数据目录 | per-user（`%LOCALAPPDATA%\Codara`），不写安装目录 |

## 文档索引

- `docs/spec/codara-spec.md` — 完整规格 v1.1（第 0～11 章 + 附录）
- `docs/spec/adr/` — ADR-01～12 架构决策
- `docs/spec/modes/` — Agent 预设提示词（minimal-longcoding.md 为默认模式权威文本）
- `docs/spec/registers/` — 兼容性风险登记表 / 依赖准入单 / 待决策待验证登记表
- `docs/spec/win7-regression.md` — Win7 专属待回归清单（沙箱不可验项）
- `shared/src/` — 双端契约唯一来源（信封/错误码/工具 schema/IPC 通道）

## 路径区分（不得混淆）

- 产品运行时读取**用户项目**里的 `AGENTS.md`（用户的项目记忆，规格 7.6）
- 本文件是 Codara **自身仓库**的开发约束

## 当前状态

- M0：视为已锁定（Electron 22.3.x / Rust 1.77.2 / MinGit 2.46.2 选型按规格执行）
- 进行中：M1-M4（对话骨架 / 极简闭环 / 专家团 / 沙箱长任务）
- 未开始：M5（索引记忆）、M6（分发）、M7
