{
  "name": "@codara/main",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "node ./build.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --root ../tests --project main --passWithNoTests"
  },
  "dependencies": {
    "@codara/contract": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^16.18.0",
    "electron": "22.3.27",
    "esbuild": "^0.20.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "zod": "^3.23.0"
  }
}
