# Win7 专属待回归清单（沙箱不可验项）

> 开发沙箱为 Linux x64。以下项目无法在沙箱验证，交付后须在 Windows 环境按此清单回归。每项附验证方法与通过口径。

## A. 运行时矩阵（规格 10 章）

| # | 项目 | 方法 | 通过口径 |
|---|---|---|---|
| A1 | Win7 SP1 x64 全补丁启动 | 纯净虚拟机安装运行 | 主窗口可交互 |
| A2 | Win7 SP1 低补丁（仅 KB4474419 链）启动 | 快照虚拟机 | 同上 |
| A3 | Win 8.1 x64 启动 | 虚拟机 | 同上 |
| A4 | Win10 1809/21H2 x64 启动 | 虚拟机 | 同上 |
| A5 | 32 位系统安装拒绝 | 安装器 | 明确提示并退出 |
| A6 | Win7 RTM 安装拒绝 | 安装器 | 提示装 SP1 并退出 |

## B. 性能指标（规格 3.4 表 3-4）

| # | 项目 | 通过口径 |
|---|---|---|
| B1 | 空闲内存（老机模式） | ≤ 250 MB |
| B2 | 空闲内存（典型） | ≤ 350 MB |
| B3 | 启动到可对话 | < 3 s（机械盘 < 5 s） |
| B4 | 工具调用往返 P95 | < 200 ms（不含命令耗时） |
| B5 | 治理管线单条 P95 | < 50 ms |
| B6 | 补丁写入后快照 | < 100 ms |
| B7 | 双核 2GHz/4GB/机械盘压测 | 上述全部达标 |

## C. 平台专属组件

| # | 项目 | 方法 | 通过口径 |
|---|---|---|---|
| C1 | DPAPI 真实加密 | 设置页存 Key → 重启 → 正常调用 | Key 不落明文，重启后可用 |
| C2 | node-pty ConPTY | terminal 工具跑 dir/echo | 输出完整、退出码正确 |
| C3 | 默认 shell=cmd.exe | terminal 工具 | `echo %CD%` 生效 |
| C4 | PowerShell 最小子集 | `Get-Content -Tail`（应拒绝/引导） | PS 2.0 限制规则生效 |
| C5 | MinGit 2.46.2 x64 | init/commit/worktree 全流程 | 全功能 |
| C6 | GBK/GB18030 文件读改写 | fixtures/legacy-gbk-crlf | 字节级保真（CRLF 不变） |
| C7 | 高 DPI / 1024×768 | 缩放 125%/150% | UI 不破版 |

## D. 崩溃恢复（M4）

| # | 项目 | 方法 | 通过口径 |
|---|---|---|---|
| D1 | 杀进程恢复 | 任务运行中 kill 进程 | 重启提示恢复，补丁不重复应用 |
| D2 | 断电恢复 | 虚拟机直接断电 | 同上 |
| D3 | 过期锁检测 | 心跳超时后重启 | 提示"恢复/终止" |

## E. 安装与分发（M6，真机项）

| # | 项目 | 方法 | 通过口径 |
|---|---|---|---|
| E1 | Win7 RTM 拒绝 | 干净 Win7（build 7600）跑安装包 | 弹"请先安装 SP1"并终止 |
| E2 | 32 位拒绝 | 32 位 Win7 跑安装包 | 弋"仅支持 64 位"并终止 |
| E3 | per-user 安装 | -DPER_USER 变体装到 %LOCALAPPDATA%\Codara | 免管理员可安装、可启动 |
| E4 | admin 安装 | admin 变体装到 %PROGRAMFILES64%\Codara | 需 UAC 提权、可启动 |
| E5 | KB 引导页 | 未装 KB4474419/KB4490628 机器 | 页面列出缺失 KB、默认勾选可跳过、不阻断安装 |
| E6 | 在线 stub 下载 | 真机联网跑 online 变体 | INetC 下载 180MB 组件包成功、失败有引导 |
| E7 | 离线包断网安装 | 拔网线跑 offline 变体 | 完整安装成功 |
| E8 | 升级备份 | 旧版有数据 → 装新版 | %APPDATA%\Codara → .bak-<ver>，rollback-manifest.json 生成 |
| E9 | 升级失败回退 | 人为中断升级 | 备份内容还原，数据不丢 |
| E10 | 卸载保留数据 | 卸载勾选保留 | 数据目录仍在；不勾选则全清 |
| E11 | 体积实测 | 四变体产物体积 | online ≤10MB；offline ≤180MB（压缩态） |
| E12 | SHA-2 签名链 | 补 KB4474419 后跑签名安装包 | UAC/签名校验通过（补丁前应失败，验证引导有效性） |
| E13 | NOTICES 随装 | 安装后检查安装目录 | NOTICES.txt 存在且含 MinGit GPLv2/字体 OFL 条目 |
